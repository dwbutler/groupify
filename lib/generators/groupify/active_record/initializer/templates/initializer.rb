Groupify.configure do |config|
  # Configure the default group and member class names.
  # Default to `nil`
  # config.group_class_name = 'Group'
  # config.member_class_name = 'User'

  # Configure the default group membership class name.
  # Defaults to 'GroupMembership'
  # config.group_membership_class_name = 'GroupMembership'

  # Configure the default group and member association names.
  # Default to `false` - specify names to auto-create them
  # config.groups_association_name = :groups
  # config.members_association_name = :members
end
